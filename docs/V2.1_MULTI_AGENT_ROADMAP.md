# PRO TRACK v2.1 — Multi-Agent Implementation Roadmap

> **This document is a hand-off spec.** Each "Phase" below is a self-contained brief you can paste into a **separate agent / isolated chat**. An agent working a phase should read only: (1) the **Global Rules** section, (2) its **own phase brief**. Everything an isolated agent needs (how the area works today, exact files, what NOT to touch) is embedded in the brief.
>
> **First action for the human:** once approved, save this file into the repo as `docs/V2.1_MULTI_AGENT_ROADMAP.md` so every agent can open it. (It currently lives only in the plan folder.)

---

## Context — why this work exists

PRO TRACK is an Electron + React (Vite) + Zustand + Firebase (Spark/free tier) study workspace. The owner asked for seven improvements after seeing a "day at a glance" timeline reference and hitting rough edges:

1. A real **day-at-a-glance** timeline that merges *everything* for a day, and a **working two-way Google Calendar sync** surfaced in the global ("All Scopes") view.
2. **Picture-in-Picture** that actually works: minimize to a small draggable floating rectangle with a live timer + pause, audio and countdown keep running, double-click restores the prior state without resetting anything.
3–4. **Optimize and inter-connect** the modules (habits, deep focus, notes, subjects, analytics, history, to-do, timetable) so you can jump between them effortlessly, they cooperate across modes/scopes, and things auto-tag.
5. **Input prompts** (check-ins, routines, quotes, anything needing an answer) take over screen-center with a **blur**, ask for input, then resume — with a visible "Esc / ✕ to close" hint and **snooze-if-unanswered**.
6. The **assistant voice** is robotic — make it natural.
7. **Every notification / toast / success chimes** for attention.

The surprising findings from exploration (already verified in code) shape the plan:

- **Google Calendar already exists** (`src/services/calendarService.js`): `calendar.events` scope, token capture, `listUpcomingEvents`/`pushSlotToCalendar`/`pullEventsToSlots`/`syncCalendar`, connect/disconnect UI, and a `/link-gcal` web handshake. **But** the OAuth token lives ~1h with a **mock** refresh token, sync is **manual**, push only covers **recurring weekly** slots, and the Netlify `gcal-sync.js` is a **501 stub**. → We harden + automate it ("live while open"), not build from zero.
- **PiP already mostly works**: the single Electron window *morphs* to a 240px always-on-top box (`ipcMain 'pip:enter'/'pip:exit'` in `electron/main.js`), rendering `src/components/focus/PipAppView.jsx` while the timer keeps ticking (`backgroundThrottling:false`). It needs **finishing**, not rebuilding. Two other PiP variants (`FloatingFocusPip.jsx`, `PipFocusWindow.jsx`) are dead on desktop.
- **`TodayAgenda.jsx` is already a vertical day timeline** → redesign + unified merge, not build-from-zero.
- **`pushIsland` (islandSlice) is the single notification funnel** but plays **no sound**; there is **no center-blur prompt** primitive yet; TTS is duplicated across 3 files with a robotic Web-Speech fallback; sounds are split across `lib/audioFX.js` + `lib/audioEngine.js` with **two different** localStorage flags.

**Locked product decisions (owner-confirmed):**
- GCal: **live while the app is open** — auto-sync on launch, on an interval, and after local edits while connected; a gentle "reconnect Google" prompt when the ~1h token lapses. No GIS silent-refresh, no server.
- GCal scope: **everything with a time** — recurring slots + one-time events + to-dos/tasks with due dates, both directions, including deletes.
- Center prompts: **only input-requiring prompts + quotes** go center-blur; informational toasts (deadlines, hydration, sync, success) stay in the Dynamic Island but now chime.

**Follow-up asks (v2.1.x — owner, post-P1 review).** After living with the merged day-view, the owner flagged day-to-day friction in Subjects, the Timetable to-do strip, quick-add ergonomics, bulk task entry, and the look of the calendar forest — "honestly, the experience was ruined." These become **P9–P13** (see the phase map). They are UX/asset refinements on top of the shipped foundation — the same free-tier, slices-pure, encrypted-fields rules apply.

8. **Subjects: exhaustive, add-anywhere, persistent rail** — the compact subjects list must show *all* subjects (scroll, not a 5-cap); a subject can be added from any state (a persistent "+" and a double-click on empty space), including the global "All Scopes" view via a mode picker; opening a subject keeps the sibling rail visible (no forced "All subjects" round-trip). → **P9**
9. **Timetable to-dos: markers + side detail pane + carry-forward** — top-of-timetable to-dos render as compact markers (not cramped full-width lines in a 76 px scroll); hovering/clicking one opens a rich detail panel in the vacant gutter; an *incomplete* to-do carries forward onto "today" each day until done (display-only, never rewriting `dueAt`). → **P10**
10. **Frictionless quick-add + at-a-glance counter** — double-click empty containers to add (to-dos, timetable cells); a clickable "N to-dos open · M events today · K overdue" counter on the calendar header. (AI "realistic timeline" recommendations are explicitly deferred by the owner.) → **P11**
11. **Per-subject AI/voice bulk task entry** — a prompt/mic box inside each subject turns "add lesson 18 to 36" (typed or dictated) into one task per lesson, via a deterministic range parser + a batched write, with a Gemini fallback for freeform phrasing. → **P12**
12. **Raster forest sprites on the calendar** — crop each tree out of the `trees.jpg` / `trees-2.jpg` sprite sheets into its own transparent PNG (and background-remove `base-shrubs.jpg`), then render those raster sprites at each calendar day-column baseline the way the previous/reference version did — replacing the current hand-drawn SVG grove. → **P13**

---

## GLOBAL RULES — every agent MUST obey these

These come from `claude.md` (repo blueprint) and the owner's coding rules. Breaking one is a failed phase.

**Architecture invariants**
1. **Zustand slices stay pure.** Slices return new objects and never run timers, I/O, audio, or side-effects. All side-effects live in hooks (`src/hooks/*`). (See `claude.md` §2, §6.)
2. **Immutability everywhere.** Never mutate state/props in place — spread into new objects/arrays.
3. **Focus ring math:** `phaseTotalSec` (focusSlice) is the *single source of truth* for ring progress. **Never** reintroduce `focusMin`/`customTimerSetting` into ring math (`FocusWidget`, `FocusMiniOverlay`, `FocusLockScreen`, `PipAppView`).
4. **YouTube embeds:** **NEVER add `&origin=`** to a focus-scene embed URL — the packaged app runs on `file://` and it breaks playback (regressed once in v1.6.0). See `src/lib/focusScenes.js`.
5. **Free-tier discipline** (`claude.md` §5): ephemeral/derived state stays local; **one bounded listener per active collection**, torn down on mode switch; coalesce writes (`arrayUnion`/`increment`/batch); bound history reads with `limit`; **no dependency on Cloud Functions or any server** (Spark plan).
6. **Lucide icons:** import **by name** (`import { Play } from 'lucide-react'`) — never `import * as`, it balloons the bundle.
7. **File size / cohesion:** keep new files 200–400 lines (800 max); many small feature-scoped files over few large ones; one service module per domain.
8. **No secrets in code.** API keys come from `secureStorage`/settings, never hardcoded.
9. **Encrypted fields:** `todos.text`, `todos.notes`, and note content are AES-GCM encrypted at rest (`cryptoService`). Read/write them only through the existing services; never log plaintext.
10. **No `console.log` in shipped code paths** (use existing `console.warn/error` only for genuine error branches, matching surrounding style).

**Build gates (run before you call a phase done — ALL must be green):**
```bash
npm run lint
npm test
npm run build
# Electron main+preload build:
# (Windows PowerShell) $env:ELECTRON="true"; npx vite build
```
- `npm test` is Vitest (95+ unit tests). Any pure logic you add needs a test in `src/lib/__tests__/` or `src/hooks/__tests__/`. Use `vi.useFakeTimers()` for anything reading `new Date()`.

**Isolation rules (prevent cross-agent merge conflicts):**
- Work on your phase's **own branch** off `development` (e.g. `feat/p3-pip`). Never commit to `master`.
- Edit **only** files in your phase's **"Files you OWN"** list. If you must touch a **shared hotspot** file (listed per phase), make **additive, anchored** edits only — do not reformat or move unrelated code.
- **Shared hotspot files** (multiple phases touch them → edit surgically, never wholesale): `src/components/layout/Dashboard.jsx` (mounts hooks/overlays), `src/components/settings/SettingsPanel.jsx`, `src/components/common/ZenOverlay.jsx`, `src/components/widgets/TimetableWidget.jsx`, `claude.md`. **Added for P9–P13:** `src/components/widgets/SubjectsWidget.jsx`, `src/components/subjects/SubjectDetail.jsx`, `src/components/timetable/TimetableGrid.jsx`, `src/services/geminiTools.js`, `src/components/focus/CalendarForest.jsx`.
- **New interaction conventions (P9–P11):** *Double-click-to-add* uses native `onDoubleClick` on an empty container where nothing competes; where a single-click already acts, reuse the 280 ms single/double discriminator in `BoardCanvas.jsx` MiniCard. *Carry-forward is display-only* — never rewrite `dueAt`; derive "carried/overdue" from `classifyDeadline()` + an `ymd()` compare at render time.
- Do **not** delete another phase's code. If something looks dead, confirm it's yours before removing.
- Commit messages: conventional commits (`feat:`, `fix:`, `refactor:`…). End every commit body with:
  `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`

---

## Phase map, dependencies & recommended waves

| Phase | Title | Owner item(s) | Depends on | Size | Status |
|---|---|---|---|---|---|
| **P0** | Unified sound module | #7 (foundation) | — | S | ✅ **merged** → `development` |
| **P1** | Day-at-a-glance timeline | #1 (view) | — | L | ✅ **merged** → `development` (`b196e23`) — **unblocks P2** |
| **P2** | Google Calendar two-way live sync | #1 (sync) | P1 (shares TimetableWidget) | L | ⬜ not started |
| **P3** | Finish Picture-in-Picture | #2 | — | M | ⬜ not started |
| **P4** | Center-blur prompt system | #5 | P0 | L | ✅ **merged** → `development` |
| **P5** | Universal chimes | #7 | P0 | S | ✅ **merged** → `development` |
| **P6** | Natural assistant voice | #6 | P4 (shares ZenOverlay) | M | ⬜ not started — **unblocked** (P4 merged) |
| **P7** | Interop: nav/context bus + tagging | #3, #4 | — | L | ⬜ not started |
| **P8** | Per-module improvements & cross-links | #3, #4 | P7 | L | ⬜ not started |
| **P9** | Subjects: exhaustive list, add-anywhere, persistent rail | #8 | — | M | ⬜ not started |
| **P10** | Timetable to-dos: markers, side detail pane, carry-forward | #9 (+#10 dbl-click) | P1 merged (shares TimetableWidget/dayAgenda) | L | ⬜ not started |
| **P11** | Frictionless quick-add + open-items counter | #10 | — | S–M | ⬜ not started |
| **P12** | Per-subject AI/voice bulk task entry | #11 | P9 merged (shares SubjectDetail) | M–L | ⬜ not started |
| **P13** | Raster forest sprites on the calendar | #12 | — | M | ⬜ not started |

**Recommended execution waves (what can run in parallel):**
- **Wave 1:** P0 (fast, unblocks P4/P5).
- **Wave 2 (parallel):** P1, P3, P7. Plus P4 and P5 once P0 is merged.
- **Wave 3:** P2 (after P1), P6 (after P4), P8 (after P7).

Merge order suggestion: P0 → P5 → P1 → P2 → P3 → P4 → P6 → P7 → P8 (merge small/foundational first to minimize rebases).

**Follow-up wave (P9–P13, owner post-P1 review):**
- **Wave 4 (parallel):** P9, P10, P11, P13 are independent of one another (P10 wants P1 merged first — already done). **P12 runs after P9** (it mounts into `SubjectDetail.jsx`, which P9 restructures).
- Follow-up merge order: append `→ P9 → P10 → P11 → P13 → P12` after the P8 merge above.

---

## Progress log

**Updated 2026-09-07.**

| Phase | Branch | Commit | Merged to `development`? | Gates |
|---|---|---|---|---|
| P0 | `feat/p0-sound` | `40095a8` | ✅ yes | lint/test/build/electron green |
| P5 | `feat/p5-chimes` | `46ad1f5` → merge `7f5a357` | ✅ yes | green |
| P4 | `feat/p4-prompts` | `9da7707` → merge `5453ea1` | ✅ yes | green (post-merge re-run: 236 tests) |
| P1 | `feat/p1-dayview` | `26a339d` | ⬜ **no — ready to merge** | green (257 tests incl. 32 new) |

`development` HEAD after this round: **`5453ea1`** (P0 + P5 + P4). `npm test` = 236 passing; `npm run lint` = 0 errors; web + electron builds green.

**What each landed phase changed (for the next agents):**
- **P0** — `src/lib/sound.js` is the single sound bank. `playSound(name)` (`chime|pop|success|habit|notify|error|prompt`), `chimeForIslandKind(kind)`, `soundsEnabled()`/`setSoundsEnabled()` on canonical `protrack:sounds` (legacy `protrack:sounds_enabled` migrated + mirrored on write). `src/lib/audioFX.js` is now a re-export shim. `audioEngine.js` untouched.
- **P5** — `useIslandCycle` plays `playSound(chimeForIslandKind(kind))` once per new `islandActive` id (`lastSoundedIdRef` guard). Skips `SELF_CHIMED = {focus, break, progress, success}` — those call sites already chime directly (`useFocusEngine`, `MicroKanban`). `SettingsPanel` sound toggle now routes through `setSoundsEnabled()`. `islandSlice` stays pure.
- **P4** — new `promptSlice` (`activePrompt` + `promptQueue`; `pushPrompt`/`resolvePrompt`/`snoozePrompt`/`dismissPrompt`/`clearPrompts`), registered in `useStore.js`. `src/components/prompt/CenterPrompt.jsx` is the blur-overlay shell (`z-[60]`, Esc/✕ capture-phase, focus-trap, `playSound('prompt')` on open, hidden during a running/locked focus session). Bodies: `CheckinPromptBody`, `RoutinePromptBody`. `useCheckIns` and `useHabitReminders.triggerHabitCue` now `pushPrompt` instead of the bottom-left card / react-hot-toast; `snoozeHabitCue()` added. `ZenOverlay` claims a `type:'quote'` queue slot while visible so nothing stacks. `CheckInCard.jsx` deleted (logic moved to `CheckinPromptBody`).

**Known follow-ups / debris (not blocking):**
- `src/components/wellness/HabitReminderToast.jsx` is now dead (P4 replaced it) — left in place; a cleanup phase or P8 can remove it.
- `checkinSlice.checkinPrompt` / `setCheckinPrompt` / `clearCheckinPrompt` are now unused (P4 uses `promptSlice`) — left as-is; not in P4's ownership.
- P4's `CenterPrompt` overlay behaviour was **not** runtime-smoke-tested (the app is auth-gated in the build environment); all four build gates pass and `promptSlice` has 11 unit tests.
- **Shared-working-tree hazard:** P1/P4/P5 ran against one checkout and `HEAD` moved between agents mid-run (recovered via `git branch -f`; no cross-contamination — each commit contains only its own files). **Give each parallel agent its own `git worktree` for the next waves.**

**Recommended next:** merge P1 (`git merge --no-ff feat/p1-dayview`), then P2 and P6 are unblocked; P3 and P7 can start anytime.

**2026-09-08 — owner post-P1 follow-up review:** added phases **P9–P13** (Subjects UX; Timetable to-do markers + side detail pane + carry-forward; frictionless quick-add + open-items counter; per-subject AI/voice bulk task entry; raster forest sprites on the calendar). Spec-only — no code written yet. See the phase map, the P9–P13 briefs, and the reconciliation note in P8.

---

# PHASE BRIEFS

---

## Phase P0 — Unified sound module (foundation)

**Branch:** `feat/p0-sound` · **Depends on:** none · **Size:** S

> ✅ **DONE & MERGED** (`40095a8`, 2026-09-07). `src/lib/sound.js` created; `audioFX.js` is a re-export shim; `sound.test.js` = 14 tests. `setSoundsEnabled` mirrors the legacy key so `audioEngine` stays in sync.

**Goal:** Create one sound API that every phase can call, and unify the two conflicting "sounds enabled" flags. No behavior change for the user yet — this only creates the primitive P4/P5 build on.

**How it works today (context):**
- `src/lib/audioFX.js` — `playChime()`, `playPop()`, `playSuccess()`, `playHabitChime()`; gated by `localStorage['protrack:sounds'] !== 'false'`. Consumed by `SessionCompleteModal`, `TodosWidget`, `NotesWidget`, `MicroKanban`, `HabitReminderToast`, `useHabitReminders`, `TimeContextPanel`.
- `src/lib/audioEngine.js` — `playChime()` (different arpeggio, used by focus complete), `playEventSound(event)` (gated by `localStorage['protrack:sounds_enabled']`), and `class MultiTrackMixer` (focus ambience). Consumed by `useFocusEngine`.
- Two flags (`protrack:sounds` vs `protrack:sounds_enabled`) drift out of sync.

**Files you OWN:**
- CREATE `src/lib/sound.js`
- CREATE `src/lib/__tests__/sound.test.js`
- EDIT `src/lib/audioFX.js` (turn into thin re-exports — see steps)

**Files you must NOT touch:** `audioEngine.js` (leave the focus chime + `MultiTrackMixer` exactly as-is), any consumer/widget, `useIslandCycle.js` (that's P5), `SettingsPanel.jsx`.

**Steps:**
1. In `src/lib/sound.js`:
   - Move the four WebAudio synth bodies from `audioFX.js` into `sound.js` unchanged (chime/pop/success/habit). Add two more: `playNotify()` (soft two-tone) and `playError()` (low descending tone) and `playPrompt()` (gentle rising tone for center prompts).
   - Export `soundsEnabled()` / `setSoundsEnabled(bool)` reading the **single** canonical key `protrack:sounds`. Include a **one-time migration**: if `protrack:sounds` is unset and `protrack:sounds_enabled === 'false'`, set `protrack:sounds='false'`.
   - Export `playSound(name)` where name ∈ `chime|pop|success|habit|notify|error|prompt` (gated by `soundsEnabled()`; wrap in try/catch — never throw into a render path).
   - Export `chimeForIslandKind(kind)` → maps island kinds to a sound: `success→success`, `progress/water/sync-online→chime`, `deadline/error→error`, `focus/break→chime`, default `info→notify`. Returns the sound name (does not play).
2. In `src/lib/audioFX.js`: replace the synth implementations with re-exports from `sound.js` (`export { playChime, playPop, playSuccess, playHabitChime } from './sound'`). This keeps all existing importers working with zero changes.
3. Test (`sound.test.js`): flag migration logic; `chimeForIslandKind` mapping table; `playSound` is a no-op when disabled (mock `AudioContext`).

**Invariants:** pure module + try/catch around every WebAudio call; do not change `audioEngine.playChime` (focus completion keeps its current sound); do not wire any UI.

**Done when:** build gates green; existing sound callers unaffected; `sound.js` exports the full bank + `chimeForIslandKind`.

---

## Phase P1 — Day-at-a-glance timeline (#1 view)

**Branch:** `feat/p1-dayview` · **Depends on:** none · **Size:** L · **Merge before P2.**

> ✅ **DONE** (`26a339d`, 2026-09-07) — **not yet merged to `development`**. `src/lib/dayAgenda.js` (`buildDayTimeline` / `summarizeDay`) + 32 tests; `TodayAgenda.jsx` redesigned (808→544 ln); `TimetableWidget.jsx` +7 additive lines wiring `tasks={[]}` (no app-wide task listener yet — `// TODO(P8)`). Deviations noted in the branch's final report: `isDueToday` replaced with a local same-day `ymd()` compare (takes an arbitrary date), note deadlines gated on `dueAt` only (not `getNoteReminders`' 48h window), `habits` param intentionally omitted (not in the `buildDayTimeline` signature), drag-to-create replaced by tap-to-add. Gates green (257 tests).

**Goal:** Redesign the single-day view into a premium vertical timeline (inspired by the reference: left time-rail, colored blocks, checkable items, "now" line, "end of day: Xh left" footer) that merges **every** dated thing for the day — timetable slots, one-time events, to-dos & tasks with due dates, completed focus sessions, note deadlines, and habit cues — into one chronological list. Must work in the global **"All Scopes"** view.

**How it works today (context):**
- `src/components/widgets/TimetableWidget.jsx` toggles `viewMode: 'week' | 'day'` (persisted `localStorage['protrack:timetable_widget_view']`). It already loads `slots`, `todos` (split into event/chip), `notes`, `sessions` and passes them down. Day view = `src/components/timetable/TodayAgenda.jsx` (06:00→midnight, `PX=0.65`, 7-day selector strip, drag-to-create, a "now" dot). Week view = `TimetableGrid.jsx` (leave it alone).
- Reusable helpers: `isSlotOnDay(slot, day)` + time constants in `src/lib/time.js`; `getUpcomingItems`/`dueAtToMinutes`/`isDueToday`/`getNoteReminders` in `src/lib/deadlines.js`; `ymd`/`getWeekDate(dayIndex, refDate)`/`dayMinToDate` in `src/lib/dates.js`; the live "now" minute from `src/hooks/useNowMinutes.js`.
- Global scope: `activeModeId === 'all'` is a real pseudo-mode. `useTimetable('all')` / `useSubjects('all')` fan out across modes and tag each item with `_modeId/_modeName/_modeColor`. Tasks come from `useTasks` (`src/hooks/useSubjects.js`).

**Files you OWN:**
- CREATE `src/lib/dayAgenda.js` (pure aggregator)
- CREATE `src/lib/__tests__/dayAgenda.test.js`
- EDIT `src/components/timetable/TodayAgenda.jsx` (redesign)
- EDIT (surgical, hotspot) `src/components/widgets/TimetableWidget.jsx` — only to also pass `tasks` (and, if not already, `sessions`/`habits`) into `TodayAgenda`, plus a "day" hero polish. Do not restructure the widget.

**Files you must NOT touch:** `TimetableGrid.jsx`, `timetableService.js` / slot doc shape, `SlotEditorModal.jsx`, calendar sync (that's P2).

**Steps:**
1. `src/lib/dayAgenda.js` — pure `buildDayTimeline({ slots, events, todos, tasks, sessions, notes, date, nowMin })` returning a chronologically sorted array of normalized items:
   `{ id, kind: 'slot'|'event'|'todo'|'task'|'note'|'session', startMin, endMin|null, title, color, subjectId?, modeId?, modeColor?, source?, done?, ref }`.
   - Slots: include when `isSlotOnDay(slot, dayOfWeek)`.
   - Events (one-time todos `type:'event'`): match `eventDate === ymd(date)`, use `eventStartMin/eventEndMin`.
   - Todos/tasks: include when `isDueToday(dueAt, date)`; place at the due time (or an "all-day / anytime" bucket when `dueAt` has no time). Reuse `dueAtToMinutes`.
   - Sessions: completed focus sessions started that day (`startedAt`), rendered as past blocks.
   - Notes: note deadlines due that day (`getNoteReminders`).
   - Sort by `startMin`, `null`/all-day items grouped into a top "anytime" bucket. Keep `done` items but mark them (for strike-through).
   - Add `summarizeDay(items, nowMin)` → `{ remainingMin, nextItem, doneCount, totalCount }` for the "end of day: Xh left" footer.
2. Redesign `TodayAgenda.jsx` to consume `buildDayTimeline`:
   - Left rail: hour labels + a continuous colored connector line; each item is a rounded card with **title, time-range, duration, and a color chip on the left edge** (match the reference). Completed items get strike-through + reduced opacity.
   - A **checkbox** on to-dos/tasks toggles done through the existing services (`todoService.updateTodo` / `subjectService.updateTask`) — reuse, don't add new writes patterns.
   - Red **"now" line + timestamp** using `useNowMinutes`; auto-scroll to now on mount.
   - Footer: "End of day: {Xh Ym} left" from `summarizeDay`.
   - Keep the existing 7-day selector strip and drag-to-create (route creation through the existing `NlQuickCapture`/`addSlot` path already wired in the widget).
   - **Global scope:** when items carry `_modeId`, show a small mode-color dot; do not hide cross-mode items.
   - Tapping a card: for now open the relevant editor/subject inline (a generic cross-module jump is added in P7 — leave a `// TODO(P7): openModule(...)` anchor).
3. Test `dayAgenda.test.js` with `vi.useFakeTimers()` fixed to a known date: slot recurrence inclusion, event-date match, due-today todo/task placement, all-day bucketing, sort order, `summarizeDay` math.

**Invariants:** pure aggregator (no reads/writes inside `dayAgenda.js`); don't change slot/todo/task doc shapes; keep the week grid untouched; free-tier (reuses already-subscribed data, adds no new listeners).

**Done when:** build gates green; day view shows a merged, chronological, checkable timeline that matches the reference feel and works in "All Scopes"; `dayAgenda.test.js` passes.

---

## Phase P2 — Google Calendar two-way live sync (#1 sync)

**Branch:** `feat/p2-gcal` · **Depends on:** P1 merged (shares `TimetableWidget`) · **Size:** L

**Goal:** Make the *existing* Google Calendar integration feel automatic and bidirectional for **everything with a time**, while the app is open, on the free tier. Surface it in the global scope + Settings.

**How it works today (context — read `src/services/calendarService.js` first):**
- Token: Firebase Google popup (web) or a desktop→browser handshake via `users/{uid}/gcal/handshake` → stored in `secureStorage['protrack:auth_core']` as `{access_token, expires_at, refresh_token:'mock_gcal_refresh_token'}`. **No real refresh.** On 401, `calFetch` clears the token; `calFetchWithRetry` currently calls `connectCalendar()` again (which on desktop *re-opens the browser* — undesirable auto-loop; you will change this to a reconnect prompt).
- Direct Calendar v3 API: `listUpcomingEvents`, `pushSlotToCalendar` (recurring weekly RRULE only), `pullEventsToSlots` (timed events → slots; skips all-day), `syncCalendar` (pull then push local-only slots). Every method first tries Netlify `/.netlify/functions/gcal-sync` which is a **501 stub** → always falls through to direct API.
- Consumers: `TimetableWidget.jsx` (connect/synced button + "From Google" list), `SlotEditorModal.jsx` (`pushSlotToCalendar` stores `googleEventId`), `SettingsPanel.jsx` (connect/unlink).

**Files you OWN:**
- EDIT `src/services/calendarService.js` (major)
- CREATE `src/hooks/useCalendarSync.js`
- CREATE `src/lib/gcalMap.js` (pure event↔item mapping) + `src/lib/__tests__/gcalMap.test.js`
- EDIT (surgical, hotspots) `src/components/settings/SettingsPanel.jsx` (GCal section: last-sync, Sync now, inbox-mode picker, auto-sync toggle), `src/components/layout/Dashboard.jsx` (mount `useCalendarSync`), `src/components/widgets/TimetableWidget.jsx` (sync status/button only)
- EDIT `claude.md` (document: `gcal/handshake`, `settings.gcalInboxModeId`, `settings.gcalAutoSync`, `googleEventId` now stored on todos/tasks too)

**Files you must NOT touch:** `TodayAgenda.jsx`/`dayAgenda.js` (P1), `timetableService`/`todoService`/`subjectService` public signatures (call them; don't rewrite), auth/login flows in `AuthContext`/`firebase.js`.

**Steps:**
1. **Stop the silent re-consent loop:** change `calFetchWithRetry` so a 401/"expired" does **not** call `connectCalendar()`. Instead throw a typed `CalendarAuthError`; the hook (step 4) surfaces a reconnect prompt.
2. **Drop the dead Netlify calls:** remove the `callSyncFunction('sync_up'/'sync_down')` attempts (they always 501) — call the direct API straight. Leave `netlify/functions/gcal-sync.js` file in place but unused (or delete if nothing else references it — verify).
3. `src/lib/gcalMap.js` (pure, tested):
   - `eventToItem(gcalEvent)` → `{ kind:'slot'|'todo', ...fields }`: timed recurring → slot (existing logic, moved here); timed one-off → a todo `type:'event'` with `eventDate/eventStartMin/eventEndMin`; all-day with a date → todo with `dueAt`. Carry `googleEventId`, `source:'gcal'`, `updated`.
   - `itemToEvent(item)` for the reverse: slot → recurring weekly (existing), one-time event → single dateTime event, todo/task with `dueAt` → timed or all-day event. Reuse `nextOccurrence` (`lib/time.js`).
   - `mergeStrategy(localUpdated, remoteUpdated)` → last-write-wins.
4. `calendarService.js` — add `syncEverything(uid, { modes, inboxModeId, slots, events, todos, tasks })`:
   - **Pull (delta):** persist a `syncToken` per calendar in `secureStorage`; call events.list with `syncToken` when present else `timeMin`; on `410 GONE` clear token → full resync. For each changed event map via `eventToItem`; `status:'cancelled'` → **delete** the mapped local item (delete propagation). New/updated GCal items land in the **inbox mode** (`settings.gcalInboxModeId`, default `activeModeId` or `modes[0].id`). Dedupe by `googleEventId`.
   - **Push:** for every local item that has a time and no `googleEventId`, create the event and write `googleEventId` back (slots via `updateSlot`, todos via `todoService.updateTodo`, tasks via `subjectService.updateTask`). For items that changed since last push (compare `updated`), PATCH.
   - **Delete reconciliation:** items carrying a `googleEventId` that no longer exist locally → delete their GCal event. (Bounded: only over items already loaded.)
   - Coalesce Firestore writes (batch where the service supports it); keep reads bounded.
5. `src/hooks/useCalendarSync.js` (mounted once in Dashboard; runs on web + desktop):
   - If `isCalendarConnected()` and `settings.gcalAutoSync !== false`: run `syncEverything` on mount, then on `setInterval` (default 5 min), on `window` `focus`, and after local mutations (debounced ~4s via a `calendarDirty` counter in `uiSlice`, bumped by the widgets that already write slots/todos/tasks — or simplest: interval + focus only for v1, note the debounce as a follow-up).
   - Push a sticky Island `kind:'sync-online'` "Calendar synced" briefly on success; on `CalendarAuthError` push a sticky `kind:'sync-offline'` "Reconnect Google Calendar" whose tap calls `connectCalendar()` (single, user-initiated — never auto).
   - **Never** run during a running/locked focus session block? (Not required — sync is background and silent; just avoid noisy Island spam: only toast on state change, not every tick.)
6. `SettingsPanel.jsx` GCal section: show connected state + **last sync time**, a **Sync now** button, an **inbox-mode** dropdown (`settings.gcalInboxModeId`), and an **Auto-sync** toggle (`settings.gcalAutoSync`). Persist via existing `userService.updateSettings`.
7. Update `claude.md` data-model + flows.

**Invariants:** **no Cloud Functions / server** (all client-side direct API); token only in `secureStorage`; **do not** request `calendar.events` at base login (keep on-demand consent); **do not** add `&origin=`; bounded reads; the reconnect prompt is the only re-auth path (no silent browser re-open). Conflict policy = last-write-wins by `updated` (documented).

**Known limitation to state in the PR:** because the token can't refresh in the background on the free tier, sync is "live while the app is open" and will ask the user to reconnect roughly hourly. This is by design (owner-approved).

**Done when:** build gates green; connecting Google then editing on either side reflects on the other while the app is open; deletes propagate; global "All Scopes" day/week views show Google items; Settings shows last-sync + controls; `gcalMap.test.js` passes.

---

## Phase P3 — Finish Picture-in-Picture (#2)

**Branch:** `feat/p3-pip` · **Depends on:** none · **Size:** M

**Goal:** Deliver the owner's exact PiP: click PiP → the app shrinks to a **small draggable floating rectangle** showing the live timer + a pause button; **audio and countdown keep running**; **double-click restores the exact prior state** (timer/audio untouched). Clean up the dead PiP variants.

**How it works today (context):**
- `src/lib/pip.js`: `enterPip()` → `window.protrack.pip.enter()` (IPC) then `setPipActive(true)`; `exitPip()` → `window.protrack.pip.exit()` then `setPipActive(false)`.
- `electron/main.js`: **one** BrowserWindow. `ipcMain.handle('pip:enter')` saves `prePipState` (bounds/fullscreen/maximized), lowers min size, exits fullscreen/maximize, sets a **240×240** bottom-right box, `setAlwaysOnTop(true,'screen-saver')`. `pip:exit` restores everything. Window state persistence refuses to save while `prePipState` is set (so the tiny box never becomes "normal"). Timer survives because `backgroundThrottling:false` and `useFocusEngine` is a hook that runs before Dashboard's PiP early-return.
- `src/components/layout/Dashboard.jsx` early-returns `<PipAppView/> + <BackgroundAudioPlayer/>` only, when `window.protrack?.isDesktop && pipActive`.
- `src/components/focus/PipAppView.jsx`: whole square is a drag region (`WebkitAppRegion:'drag'`); hover shows mute/expand/close/play-pause. **Bugs:** the Expand (`Maximize2`) button and the Close (`X`) button **both** call `exitPip` (no distinction); there is **no double-click-to-restore**; controls only appear on hover.
- Dead on desktop: `FloatingFocusPip.jsx` (guarded off when `isDesktop`; buggy hardcoded position), `PipFocusWindow.jsx` (Document-PiP, guarded off when `isDesktop`, contains stray desktop calls). `FocusMiniOverlay.jsx` is a separate in-app pill (keep, minor dead `setPipActive` sub).

**Files you OWN:**
- EDIT `src/components/focus/PipAppView.jsx`
- EDIT `electron/main.js` (only the `pip:enter`/`pip:exit` handlers + any bounds constants) and `electron/preload.js` (only if you add an IPC method)
- EDIT `src/lib/pip.js`
- EDIT `src/components/focus/PipFocusWindow.jsx` and `src/components/focus/FloatingFocusPip.jsx` (scope down / gate to web-only or remove desktop dead code)
- EDIT (surgical, hotspot) `src/components/layout/Dashboard.jsx` — only the PiP early-return / mount region if needed.

**Files you must NOT touch:** `focusSlice.js` ring math, `useFocusEngine.js` tick/audio logic (PiP must not change how the timer or ambience runs), `SessionCompleteModal`, `FocusLockScreen` (except its existing "PiP Mode" button, which already calls `enterPip` — leave it).

**Steps:**
1. **Double-click to restore:** add `onDoubleClick={exitPip}` to the PiP square in `PipAppView.jsx`. Ensure double-click is distinguishable from drag (the drag region is `WebkitAppRegion:'drag'`; a double-click still fires — verify in Electron; if drag swallows it, add a small non-drag hit target or listen at the window level). Keep `exitPip` = restore prior window state (already implemented in `pip:exit`).
2. **Distinguish Expand vs Close:** give the two buttons distinct behavior. Recommended: **Expand** (`Maximize2`) = `exitPip` (restore to prior full state); **Close** (`X`) = a new `pip:exit` variant that restores the window **and pauses** the session (`pause()`), OR simply remove the redundant second button and keep one clear "Restore" control + the double-click. Pick one and make the tooltips honest.
3. **Make the timer + pause always legible:** show the countdown at rest (already), and surface a small **pause/resume** affordance without requiring hover (e.g. a always-visible bottom bar with `mm:ss` + a pause dot), while keeping full controls on hover. Respect `WebkitAppRegion:'no-drag'` on all buttons.
4. **Confirm audio continuity:** verify `BackgroundAudioPlayer` (inline in Dashboard) keeps playing during PiP (it should — it un-mounts only when `focusLocked && !pipActive`). Do not change its guard.
5. **Bounds polish (optional):** allow the PiP box to be a touch larger/resizable if trivial, but do **not** regress the "never persist tiny bounds as normal" logic in `main.js`.
6. **Dead-code cleanup:** in `FloatingFocusPip.jsx` and `PipFocusWindow.jsx`, either (a) delete them and their mounts in `Dashboard.jsx`, or (b) keep `PipFocusWindow` strictly for real browsers (non-Electron) and delete `FloatingFocusPip`. Remove the stray `window.protrack?.window?.minimize/restore` calls from `PipFocusWindow`. Keep desktop behavior 100% on the window-morph path.

**Invariants:** timer and audio must **never** reset on enter/exit PiP; ring uses `phaseTotalSec`; restore returns the *exact* prior window geometry/fullscreen; no new BrowserWindow (stick with the morph approach); free-tier/no-server.

**Verification (manual, desktop):** start a focus session → click PiP → window shrinks to a draggable always-on-top box, timer counts down, audio continues → drag it → double-click → window restores to prior size/fullscreen with timer/audio unbroken → pause/resume from the PiP works.

**Done when:** build gates green; the manual flow above passes; no dead PiP variant renders on desktop.

---

## Phase P4 — Center-blur prompt system (#5)

**Branch:** `feat/p4-prompts` · **Depends on:** P0 merged · **Size:** L

> ✅ **DONE & MERGED** (`9da7707`, merge `5453ea1`, 2026-09-07). New `promptSlice` (registered in `useStore.js`), `src/components/prompt/{CenterPrompt,CheckinPromptBody,RoutinePromptBody}.jsx`, `promptSlice.test.js` = 11 tests. `useCheckIns` + `useHabitReminders.triggerHabitCue` now `pushPrompt`; `snoozeHabitCue()` added; `ZenOverlay` shares the queue as `type:'quote'`; `CheckInCard.jsx` deleted. **Left behind:** `HabitReminderToast.jsx` now dead (not deleted — outside P4 ownership); `checkinSlice.checkinPrompt` fields now unused. Overlay not runtime-smoke-tested (auth-gated env); gates green.

**Goal:** Any prompt that needs an answer — check-ins, routine/habit cues, low-energy nudges, and quotes — takes over **screen-center with a full blur**, asks for input, then resumes normal work. A visible hint says **"Esc / ✕ to close"**, and closing without answering **snoozes**. Informational toasts stay in the Dynamic Island (untouched here; they get sound in P5).

**How it works today (context):**
- **No center-blur prompt primitive exists.** Nearest: `src/components/ui/Modal.jsx` (portal + centered + `backdrop-blur`, but no Esc handling, no hint, no queue/snooze) and `src/components/common/ZenOverlay.jsx` (full-screen center + Esc/Space/X + a hint string — good reference; also renders quotes).
- **Check-ins:** `src/hooks/useCheckIns.js` sets `checkinPrompt` in `checkinSlice`; `src/components/checkin/CheckInCard.jsx` renders it as a **bottom-left glass card** with inputs; dismissing calls `snoozeCheckins()` (localStorage `protrack:checkin:snoozedUntil`, 90 min). Gating (`shouldPrompt`) already skips focus sessions + 90s settle.
- **Routine/habit cues:** `src/hooks/useHabitReminders.js` + `src/components/wellness/HabitReminderToast.jsx` (a react-hot-toast with Done/Snooze). These "require input."
- **Quotes:** `ZenOverlay.jsx`, triggered by idle (`useIdleDetection`), already center + Esc/Space/X.
- Dashboard mounts `CheckInCard`, `ZenOverlay`, and the habit reminder machinery.
- Island z-40; Modal/Zen/Sheet z-50. New center prompt should sit **above** the Island (e.g. z-[60]).

**Files you OWN:**
- CREATE `src/store/slices/promptSlice.js` (pure queue: `activePrompt`, `promptQueue`, `pushPrompt`, `dismissPrompt`, `resolvePrompt`, `snoozePrompt`)
- CREATE `src/components/prompt/CenterPrompt.jsx` (the blur-overlay shell: backdrop-blur, Esc + ✕, the "Esc / ✕ to close — snoozes if unanswered" hint, focus-trap, renders the active prompt's body; **plays `playSound('prompt')` from `lib/sound.js` on open**)
- CREATE prompt bodies: `src/components/prompt/CheckinPromptBody.jsx`, `src/components/prompt/RoutinePromptBody.jsx` (reuse/port `HabitReminderToast` content), and route quotes through it or keep `ZenOverlay` as the quote body.
- EDIT `src/store/useStore.js` (register `createPromptSlice`)
- EDIT `src/hooks/useCheckIns.js` (push a prompt instead of setting the bottom-left card)
- EDIT `src/hooks/useHabitReminders.js` (route the cue into the center prompt queue)
- EDIT (surgical, hotspot) `src/components/layout/Dashboard.jsx` (mount `<CenterPrompt/>`; remove/retire `<CheckInCard/>` + `HabitReminderToast` rendering), `src/components/common/ZenOverlay.jsx` (make it a prompt body or gate it through the queue so a quote and a check-in never stack — coordinate with P6 which also edits ZenOverlay; P4 lands first).

**Files you must NOT touch:** `islandSlice.js` / `DynamicIsland.jsx` / `useIslandCycle.js` (informational toasts stay as-is; P5 owns their sound), `checkinService.js` / `checkin.js` logic (reuse `shouldPrompt`, `selectQuestion`, `saveCheckinAnswer` unchanged).

**Steps:**
1. `promptSlice.js` — a small FIFO like `islandSlice` but for **blocking** prompts:
   `{ activePrompt, promptQueue }` with `pushPrompt({ type:'checkin'|'routine'|'quote', payload, snoozeMs, dismissible:true })`, `resolvePrompt()` (answered → advance queue), `snoozePrompt()` (unanswered dismissal → run the type's snooze then advance), `dismissPrompt(id)`. **Pure.** Only one prompt visible at a time; new prompts queue behind the active one.
2. `CenterPrompt.jsx` — portal overlay `fixed inset-0 z-[60]` with `backdrop-blur-xl bg-black/40`; centered glass panel; **Esc** and a top-right **✕** both call the type's snooze-dismiss; a persistent small hint line **"Press Esc or ✕ to close · snoozes until later"**; focus-trap + `autoFocus` the primary input; on mount `playSound('prompt')`. Renders the active prompt's body component by `type`.
3. Bodies:
   - `CheckinPromptBody` = the existing `CheckInBody` UI (port the JSX from `CheckInCard.jsx`) — inputs, "Save", "Make it a to-do", trend strip. On save call `saveCheckinAnswer(...)` (unchanged) then `resolvePrompt()`. On dismiss → `snoozeCheckins()` (reuse) then `snoozePrompt()`.
   - `RoutinePromptBody` = port `HabitReminderToast` (Mark Done / Snooze) → its Done calls the existing habit toggle; Snooze reuses its 10-min logic.
   - Quotes: simplest is to keep `ZenOverlay` as the visual but have it register as a `type:'quote'` prompt so it shares the queue + snooze semantics (idle-triggered). Ensure a quote never covers a pending check-in (queue handles this).
4. Wire triggers:
   - `useCheckIns.js`: where it currently calls `st.setCheckinPrompt(...)`, instead `st.pushPrompt({ type:'checkin', payload:{ slot, question }, snoozeMs: 90*60*1000 })`. Keep all gating (`shouldPrompt`, focus-skip, settle) intact.
   - `useHabitReminders.js`: where it currently fires the toast, `pushPrompt({ type:'routine', payload:habit, snoozeMs: 10*60*1000 })`.
5. Dashboard: mount `<CenterPrompt/>` (once). Remove the `<CheckInCard/>` mount and the habit-toast rendering. (You may delete `CheckInCard.jsx` after porting, or leave it unused — prefer delete to avoid confusion; keep `snoozeCheckins` export in `useCheckIns.js`.)
6. Respect the existing "never during a running/locked focus session" rule — the prompt queue must not pop over the focus lock screen (check `focusLocked`/`status==='running'` before showing, mirroring today's gating).

**Invariants:** slices pure; one prompt at a time; unanswered close always snoozes (never lost silently); quotes/check-ins/routines never stack; do not steal the keyboard during a focus session; informational Island toasts unchanged.

**Verification:** trigger a check-in (e.g. temporarily loosen the settle window / clock) → screen blurs, prompt centers, plays a chime, Esc snoozes, answering resolves and resumes; idle → quote appears via the same shell.

**Done when:** build gates green; check-ins, routine cues, and quotes all render through `CenterPrompt` with Esc/✕ + snooze + chime; informational toasts still use the Island.

---

## Phase P5 — Universal chimes (#7)

**Branch:** `feat/p5-chimes` · **Depends on:** P0 merged · **Size:** S

> ✅ **DONE & MERGED** (`46ad1f5`, merge `7f5a357`, 2026-09-07). `useIslandCycle` chimes once per new `islandActive` id (`lastSoundedIdRef` guard), skipping `SELF_CHIMED = {focus, break, progress, success}` (those call sites already chime directly — `useFocusEngine`, `MicroKanban`). `SettingsPanel` toggle routes through `setSoundsEnabled()`. `islandSlice` untouched (stays pure). No new tests (hook; mapping covered by `sound.test.js`).

**Goal:** Every notification / toast / success makes a fitting sound. The single funnel is `pushIsland`; add sound at the render side (not the pure slice).

**How it works today (context):**
- `src/store/slices/islandSlice.js` is the single notifier funnel (`pushIsland`) but is **pure and silent** (must stay pure — do not add audio there).
- `src/hooks/useIslandCycle.js` owns the auto-advance timer keyed on the active event; `src/components/island/DynamicIsland.jsx` renders it. Neither plays sound today.
- `src/lib/sound.js` (from P0) exports `playSound(name)` + `chimeForIslandKind(kind)`.

**Files you OWN:**
- EDIT `src/hooks/useIslandCycle.js` (play `chimeForIslandKind(active.kind)` when a **new** `islandActive` appears — guard against replay on re-render by tracking the last-sounded id)
- EDIT (surgical) `src/components/settings/SettingsPanel.jsx` — make the existing sound toggle write the unified `protrack:sounds` key via `setSoundsEnabled` (so one toggle governs everything)

**Files you must NOT touch:** `islandSlice.js` (keep pure), the P4 `CenterPrompt` (it already chimes on open).

**Steps:**
1. In `useIslandCycle.js`, add an effect: when `islandActive?.id` changes to a new id, call `playSound(chimeForIslandKind(islandActive.kind))`. Track `lastSoundedIdRef` to avoid double-play. Respect `soundsEnabled()` (already gated inside `playSound`).
2. Ensure `SessionCompleteModal` / habit / kanban existing chimes still fire (they import from `audioFX` which P0 re-exports — no change needed) but now share the single flag.
3. Settings: point the sound on/off control at `setSoundsEnabled`, reflecting `soundsEnabled()`.

**Invariants:** slice stays pure; no double-chime; one localStorage flag governs all sound; don't chime for silent-by-design events if any (e.g. very frequent progress updates — if `kind:'progress'` fires often, debounce or map it to no-sound).

**Done when:** build gates green; pushing any Island event plays a fitting sound once; a single Settings toggle mutes everything.

---

## Phase P6 — Natural assistant voice (#6)

**Branch:** `feat/p6-voice` · **Depends on:** P4 merged (shares `ZenOverlay`) · **Size:** M

**Goal:** Replace the robotic Web-Speech fallback with a natural voice, and centralize the duplicated TTS into one utility.

**How it works today (context):**
- TTS is copy-pasted in **three** files: `src/components/ai/HandsFreeTab.jsx`, `src/components/ai/BackgroundHandsFree.jsx`, `src/components/common/ZenOverlay.jsx`. Each tries **ElevenLabs** (voice `21m00Tcm4TlvDq8ikWAM`, `eleven_multilingual_v2`, key from `geminiService.getElevenLabsKey()` → `secureStorage['protrack:elevenlabs_key']`) then falls back to **Web Speech** (`SpeechSynthesisUtterance`) with a crude "first `en*` voice, rate 0.85/0.75, pitch 0.9/0.85" heuristic — **that fallback is the robotic voice**.
- OpenAI key also exists (`geminiService.getOpenAIKey()`), unused for TTS.

**Files you OWN:**
- CREATE `src/lib/tts.js` (single `speak(text, { lang })` + `stopSpeaking()` + `getPreferredVoice()`), and `src/lib/__tests__/tts.test.js`
- EDIT `src/components/ai/HandsFreeTab.jsx`, `src/components/ai/BackgroundHandsFree.jsx`, `src/components/common/ZenOverlay.jsx` — replace their inline speak logic with `tts.speak`
- EDIT (surgical) `src/components/settings/SettingsPanel.jsx` — optional voice picker + provider preference

**Files you must NOT touch:** `geminiService.js` key getters (call them), the P4 prompt system.

**Steps:**
1. `src/lib/tts.js` — one tiered `speak(text, opts)`:
   - **Tier 1 ElevenLabs** if `getElevenLabsKey()` (keep current config).
   - **Tier 2 OpenAI TTS** if `getOpenAIKey()` (use OpenAI's speech endpoint, a natural voice) — optional but recommended mid-tier.
   - **Tier 3 Web Speech, dramatically improved:** pick the best available voice by preferring **online/neural** voices by name (e.g. contains "Natural", "Online", "Aria", "Jenny", "Guy", "Google"), matching `opts.lang`; fall back gracefully. Use **rate ~1.0, pitch 1.0** (not 0.85/0.9). Cache the chosen voice; handle async `voiceschanged`.
   - Expose `stopSpeaking()` (cancel Audio + `speechSynthesis.cancel()`), and a `setVoicePreference()` persisted in localStorage.
2. Swap the three consumers to `tts.speak` / `tts.stopSpeaking`; delete their inline `fallbackWebSpeech`/`speakQuote` bodies. Preserve each caller's existing gating (e.g. `settings.zenVoiceEnabled` for quotes).
3. Optional Settings: a voice dropdown (from `speechSynthesis.getVoices()`), stored via `setVoicePreference`.
4. Test the voice-selection ranking (mock `getVoices()`), lang matching, and provider tier order.

**Invariants:** never hardcode keys; graceful fallback chain (a missing key or offline must degrade to Web Speech, never throw into UI); centralized — no TTS logic left inline in the three components.

**Done when:** build gates green; with no API key, the fallback uses a natural neural OS voice at rate/pitch ~1.0; the three callers share `tts.js`; `tts.test.js` passes.

---

## Phase P7 — Interoperability: nav/context bus + unified tagging (#3, #4 core)

**Branch:** `feat/p7-interop` · **Depends on:** none · **Size:** L

**Goal:** Build the two primitives that let modules cooperate: (a) a **cross-module navigation/context bus** so any item can open another module with context, and (b) a **unified, auto-populated tag layer** across todos/tasks/notes/slots/subjects.

**How it works today (context):**
- Navigation is a **single widget board, no routing** (`Dashboard.jsx` → `BoardCanvas.jsx`): single-click a mini card swaps it into the right slot; double-click `maximizeWidget(id)`; hero mode has a `DockChip` rail. Registry: `src/components/widgets/widgetRegistry.js` (`timetable, todos, habits, focus, notes, subjects, analytics, ledger`).
- The **only** cross-module deep-link is `openFocus(focusContext)` in `uiSlice` (Subject→Focus, Slot→Focus, Zen rec→Focus), consumed by `FocusPanel.jsx`.
- **Modes = scope**; `activeModeId === 'all'` is the global view. Scoping is done **3 inconsistent ways**: per-mode subcollections (subjects/tasks/slots), `modeId` field + client filter (todos/notes), fully global (habits — no `modeId`).
- **Tagging is greenfield:** dormant `notes.tags[]` (written, never read/set in UI), subject `flags[]` (user-facing, `SubjectDetail`), a per-slot free-text `tag`+`tagStyle`. No shared taxonomy, no `#hashtag` parsing, no auto-tag.

**Files you OWN:**
- CREATE `src/store/slices/navSlice.js` — generalize `openFocus` into `moduleContext` + `openModule(widgetId, context)` + a small back-stack (`navBack()`), and register in `useStore.js`. (Keep `openFocus` working by delegating.)
- CREATE `src/lib/tags.js` (+ test) — `parseHashtags(text)`, `normalizeTag(s)`, `deriveAutoTags({ text, subjectName, modeName })`, `mergeTags(existing, derived)`. Pure.
- CREATE `src/components/common/ScopeChip.jsx` / a tiny `TagPill.jsx` shared UI (optional).
- EDIT `src/store/useStore.js` (register nav slice)
- EDIT `src/components/widgets/BoardCanvas.jsx` (consume `moduleContext` to focus the target widget on the right item)

**Files you must NOT touch (in P7):** the individual module widgets' internals (that's P8), calendar/PiP/prompt code.

**Steps:**
1. `navSlice.js`: `moduleContext: { widgetId, itemType, itemId, subjectId?, date?, tag? }`, `openModule(widgetId, ctx)` (sets context + `maximizeWidget`/swap), `clearModuleContext`, `navBack()` (bounded stack, e.g. last 10). Make `openFocus` call `openModule('focus', ...)` under the hood so nothing breaks.
2. `lib/tags.js`: `#hashtag` extraction + normalization; `deriveAutoTags` = subject name slug + mode name + parsed hashtags, de-duped/normalized. Pure + tested.
3. `BoardCanvas.jsx`: when `moduleContext` is set, ensure the target widget is shown (swap/maximize) and pass the context down as a prop so P8's widgets can scroll-to/highlight the item. Add a lightweight "back" affordance using `navBack()`.
4. Document the tag convention and the nav contract at the top of each new file so P8 agents can consume them without this chat.

**Invariants:** slices pure; `openFocus` behavior preserved exactly; tags are additive metadata (never change existing doc shapes destructively — you only *add* a `tags[]` where missing, mirroring the dormant `notes.tags[]`); free-tier (no new listeners; tag index is derived client-side).

**Done when:** build gates green; `openModule` can focus any widget with context; `tags.js` tested; `BoardCanvas` honors `moduleContext`. No user-visible module changes yet (P8 wires them).

---

## Phase P8 — Per-module improvements & cross-links (#3, #4 detail)

**Branch:** `feat/p8-modules` · **Depends on:** P7 merged · **Size:** L (sub-divisible per module across agents)

**Goal:** Make each module better and interoperable using P7's `openModule` + `tags.js`. This phase is a **checklist you can split across several agents**, one module each — but all must import the P7 primitives and obey the same tag convention.

**Shared rules for every sub-task:**
- Use `openModule(widgetId, ctx)` for jumps; never invent a second navigation mechanism.
- When creating/editing todos/tasks/notes, call `deriveAutoTags` + `mergeTags` and persist `tags[]` through the existing service (`todoService`/`subjectService`/`noteService`). Parse `#hashtags` from text on save.
- Do not change Firestore paths or break mode scoping; when adding cross-links, respect `activeModeId === 'all'`.

**Per-module checklist (each bullet ≈ one agent-sized task):**
- **To-do** (`TodosWidget.jsx`): add `#hashtag` parsing + auto-tags on create; "Schedule" action → creates a timetable slot/event via `addSlot`/event path and jumps to Timetable; "Focus this" → `openModule('focus', {todo})`; tag filter chips.
- **Subjects** (`SubjectsWidget.jsx`/`SubjectDetail.jsx`): migrate `flags[]` UI to the shared tag pills; "View analytics" → `openModule('analytics', {subjectId})`; "History" → `openModule('ledger', {subjectId})`; keep the existing Subject→Focus.
- **Timetable** (`TimetableWidget.jsx`/`TodayAgenda.jsx`): make P1's card taps call `openModule` (fill the `// TODO(P7)` anchor) — slot→subject/focus, todo→to-do editor.
- **Notes** (`NotesWidget.jsx`): **activate the dormant `notes.tags[]`** (read + set in UI); link a note to a subject/todo; `#hashtag` parsing; note-deadline already surfaces in Timetable (keep).
- **Habits** (`HabitsWidget.jsx`): "See trends" → `openModule('analytics', {habitId})`; optionally add an opt-in `modeId` so habits can be scoped (default remains global — do not force-migrate existing docs).
- **Deep Focus** (`FocusWidget.jsx`): allow starting focus from a to-do/task (extend `openFocus` payload); ensure completed sessions link back to their subject in History/Analytics.
- **Analytics** (`AnalyticsWidget.jsx`/`AnalyticsCharts.jsx`): drill-down — clicking a chart segment `openModule`s the source (subject/habit). Keep the lazy `vendor-charts` split (`React.lazy`) — do **not** import Recharts eagerly.
- **History/Ledger** (`LedgerWidget.jsx`): clicking an entry jumps to its subject/task via `openModule`. Keep the bounded 50-read.

> **Reconciled with the P9–P13 follow-ups (do those first).** The Subjects list/rail/add-anywhere work is now **P9**, the Timetable marker/detail-pane/carry-forward work is **P10**, the double-click-to-add + counter is **P11**, and the calendar forest art is **P13**. P8 must **not** re-do that UI — it only layers the tag + `openModule` cross-links on top. The Timetable `// TODO(P7)` card-tap→`openModule` anchor stays in P8; P10 owns the shelf/detail-pane.

**Invariants:** all Global Rules; analytics stays lazy-loaded; tags additive; no new unbounded listeners; each sub-task independently build-green.

**Done when:** build gates green; you can travel Subject → Tasks → Focus → Analytics → History and back via in-app jumps; creating a to-do/note/task auto-tags it and `#hashtags` work; tag filtering surfaces cross-module items.

---

## Phase P9 — Subjects: exhaustive list, add-anywhere, persistent rail (#8)

**Branch:** `feat/p9-subjects` · **Depends on:** none · **Size:** M

**Goal:** Fix the three Subjects pain points: the compact list must show **all** subjects (scroll, no
5-cap); a subject can be added from **any** state and scope; opening a subject keeps the sibling rail
visible (no "All subjects" round-trip).

**How it works today (context):** `SubjectsWidget.jsx` has two branches on `isHero`. Compact caps at
`subjects.slice(0, 5)` (`:137`) inside an `overflow-y-auto` container, so subjects 6+ are simply not
rendered (not scrollable-to). Add triggers: an empty-state link in both variants (`:85`); a "+ Subject"
header button that is **hero-only + non-`fullDetail`** (`:62-77`); `openCreate` hard-blocks the `all`
scope with `toast.error('Please select a specific mode to add subjects.')`. The sibling list + the
"All subjects" back button live **here** (not in `SubjectDetail.jsx`); a `fullDetail` boolean (`:18`)
toggles a sidebar-less full-screen (`:90-98`), and only `setFullDetail(false)` returns. `selectedId`
(`:17`) tracks selection; a compact-row single click runs `pickCompact`→`maximizeWidget('subjects')`.
`all` scope is handled by `useSubjects('all')` tagging `_modeId/_modeName/_modeColor`; writes route
through `targetModeId` in `SubjectDetail.jsx:27`. Create = `subjectService.addSubject(uid, modeId, {…})`.

**Files you OWN:** EDIT `src/components/widgets/SubjectsWidget.jsx` (primary); EDIT
`src/components/subjects/SubjectEditorModal.jsx` (accept an optional mode picker for `all` scope); EDIT
`src/components/subjects/SubjectDetail.jsx` only if the rail layout requires it.

**Files you must NOT touch:** `useSubjects.js` fan-out, `subjectService` signatures, `MicroKanban`
internals, `BoardCanvas` maximize behavior.

**Steps:**
1. **#1 exhaustive list** — remove `.slice(0, 5)`; map **all** `subjects` in the existing scrollable
   container so 6+ are reachable by scroll. Keep the card height bounded (already `flex-1
   overflow-y-auto`). Optional small "N subjects" count. Hero list is already uncapped — leave it.
2. **#2 add-from-anywhere** — add a **persistent** add affordance to the compact (non-empty) card: a
   small "+ Subject" tile/row pinned at the bottom of the list (not hero-gated), plus native
   `onDoubleClick` on the list's empty area → `openCreate`. Un-gate the trigger so it is reachable in
   compact and in full detail. Replace the `all`-scope `toast.error` with a **mode picker** in
   `SubjectEditorModal` (default `activeModeId || modes[0].id`) so adding works from the global view.
3. **#3 persistent rail** — remove the sidebar-less `fullDetail` layout. In hero, always render a
   **two-pane** layout: a left rail (all subjects, scrollable, current selection highlighted) + the
   expanded `SubjectDetail` on the right. A rail click just swaps `selectedId` — no toggle, no "All
   subjects" trip. Make the rail collapsible (a chevron, default expanded) for users who want maximum
   detail width. Keep the `!isHero → reset` effect and the `targetModeId` write routing.
4. **#4 (subjects)** — the empty-area `onDoubleClick` from step 2 is the subjects double-click-to-add.

**Invariants:** immutability; `all`-scope write routing intact (`_modeId`/`targetModeId`); no
`subjectService` signature changes; `BoardCanvas` single/double maximize untouched.

**Done when:** the compact card scrolls through all subjects (not 5); a subject can be added from
compact, hero, full-detail, and `all` scope; opening a subject keeps the rail visible and one-click
switchable.

---

## Phase P10 — Timetable to-dos: markers, side detail pane, carry-forward (#9)

**Branch:** `feat/p10-timeline-todos` · **Depends on:** P1 merged · **Size:** L

**Goal:** Replace the cramped top-of-timetable to-do lines with **compact markers**, show a rich
**detail pane in the vacant gutter** on hover/click, and make **incomplete to-dos carry forward** onto
today until done — display-only, no `dueAt` writes. Also add double-click-to-add on empty time cells.

**How it works today (context):** the week grid "all-day / tasks shelf" is `TimetableGrid.jsx:408-485`
— it has **no numeric cap**; the "~3 visible" behaviour is purely CSS `max-h-[76px] overflow-y-auto` on
the shelf (that nested scroll is the friction). Chips are amber, full-column-width, with a native
`title` tooltip only; click starts a 30-min focus. The day view "anytime" bucket is
`TodayAgenda.jsx:312-342` (AgendaCards, scroll, native title). `dayAgenda.js`
(`buildDayTimeline`/`summarizeDay`, pure) has **no rollover** — a dated todo shows only on its exact due
day (`sameLocalDay`); undated todos pile under "today" via `isCurrentToday`. `anchoredMinutes` →
`dueAtToMinutes` pins a 7 am todo at 7 am. `deadlines.js`: `classifyDeadline` →
`overdue|due_today|due_soon|upcoming`, plus `isDueToday`, `dueAtToMinutes`. `dates.js`: `ymd`,
`getWeekDate`.

**Files you OWN:** EDIT `src/components/timetable/TodayAgenda.jsx`; EDIT
`src/components/timetable/TimetableGrid.jsx` (the top shelf only → markers + double-click cell add; P10
takes shelf ownership now that P1 is merged); CREATE
`src/components/timetable/ItemDetailPopover.jsx`; EDIT (surgical) `src/lib/dayAgenda.js` +
`src/lib/__tests__/dayAgenda.test.js` (add an opt-in carry-forward path + tests); EDIT (surgical,
hotspot) `src/components/widgets/TimetableWidget.jsx` (pass a carry flag only — do not restructure).

**Files you must NOT touch:** `timetableService` / slot doc shape, `todoService`/`subjectService`
signatures, calendar sync (P2), the grid's slot/event blocks beyond the shelf.

**Steps:**
1. **#5a markers** — in both the week shelf and the day "anytime" bucket, render each pending to-do/task
   as a compact numbered pill (badge + a 1-line truncated label; a numbered dot when space is tight).
   Show the first N as chips + a "+K more" pill that expands; the full list lives in the detail pane, not
   a 76 px nested scroll.
2. **#5b detail pane** — `ItemDetailPopover.jsx`: on marker hover (and click/tap for touch) show a panel
   anchored to the **vacant gutter** beside the grid (right if room, else left) with title, full text,
   due date/time, subject/mode, status, a "carried over from {date}" line when applicable, and quick
   actions (complete / open / focus). Debounce hover-in/out; keyboard-focusable markers open the same
   panel.
3. **#5c carry-forward (display-only)** — add an opt-in path in `buildDayTimeline` and the shelf's
   `dayTopTasks` filter so an **incomplete** dated item whose due date is **before today** also appears
   on **today** (only the current day accumulates overdue — not arbitrary browsed dates), tagged
   `carriedFrom:<ymd>` and styled overdue, via `classifyDeadline()==='overdue'` + an `ymd()` compare
   against the view date. **Never rewrite `dueAt`.** Completing the item stops the carry. Tests
   (`vi.useFakeTimers()`): overdue-incomplete carries to today; done does not; future-dated does not;
   the exact due day still shows it once.
4. **#5d placement** — keep the 7-am-on-its-day anchoring; carried items render in the top
   overdue/anytime bucket of *today* (not re-anchored to a random time), with their original time shown
   in the detail pane.
5. **#4 (timetable)** — add `onDoubleClick` on empty cells / the day-header strip (which already
   single-click `onSelect`) → open add-at-that-time. Prefer the header-strip / top-shelf cells (no drag
   conflict); for week-grid body cells, disambiguate from the existing pointer-drag select by mirroring
   the 280 ms pattern from `BoardCanvas.MiniCard`.

**Invariants:** `dayAgenda.js` stays pure (carry is a pure fn of the view date + now); no `dueAt`
writes; free-tier (reuses already-subscribed data, no new listeners); the grid's slot/event blocks
unchanged; P1's complete-checkbox wiring kept.

**Done when:** top-of-timetable to-dos render as compact markers with a rich side detail pane on
hover/click (no 76 px nested scroll); incomplete overdue to-dos carry onto today until done;
double-clicking an empty time cell starts an add; the carry-forward tests pass.

---

## Phase P11 — Frictionless quick-add + open-items counter (#10)

**Branch:** `feat/p11-quickadd-counter` · **Depends on:** none · **Size:** S–M

**Goal:** Double-click empty containers to add (to-dos, and the timetable cells P10 does not cover), and
add a live, clickable "N to-dos open · M events today · K overdue" counter to the calendar header.

**How it works today (context):** `TodosWidget` add = an input capsule + `parseCapture`; the card
`onDoubleClick` already promotes backlog→doing (taken — don't collide with it). `openCount =
backlog+doing` is computed locally (`:1015`) and shown as its own subtitle. `TimetableWidget` already
has `activeTodos` (mode-scoped non-done) + `eventTodos`. `WidgetFrame`'s subtitle already renders
dynamic text (`${slots.length} sessions/week · ${mode}`) — the natural counter home; `TopBar.jsx` is
**not mounted** (the global header is `DynamicBranding`). `summarizeDay` yields `doneCount/totalCount`.

**Files you OWN:** EDIT (surgical) `src/components/widgets/TodosWidget.jsx` (double-click a column's
empty body / a "+ tile" → focus the add input for that column; leave the card double-click alone);
CREATE `src/lib/counts.js` + `src/lib/__tests__/counts.test.js` (pure
`openItemCounts({ todos, events, date })` → `{ openTodos, eventsToday, overdue }`, reusing
`classifyDeadline`/`ymd`); EDIT (surgical, hotspot) `src/components/widgets/TimetableWidget.jsx` (render
the counter pill in the `WidgetFrame` subtitle / header near the sync area; click → switch to day view).

**Files you must NOT touch:** the todo encryption path, `todoService` signatures, calendar-sync
internals.

**Steps:**
1. **#4 (todos)** — double-click on a column's empty body and/or a persistent "+ add" tile at the top of
   the column → focus/open the add input, pre-targeted to that column. Keep the existing card
   double-click semantics.
2. **#6 counter** — `counts.js` pure helper; render a compact clickable pill "N open · M today · K
   overdue" in the timetable header, sourced from the already-subscribed `activeTodos`/`eventTodos` (no
   new reads). Click → day view (or maximize todos). Leave a `// TODO(AI): realistic-timeline
   suggestions` anchor (owner deferred the AI recommendations).
3. Tests for `counts.js` with a fixed clock.

**Invariants:** pure count helper + test; no new listeners; free-tier; the todo double-click semantics
unchanged.

**Done when:** double-clicking empty todo-column space starts an add; a live counter pill in the
timetable header shows open todos + today's events (+ overdue), clickable to the day view;
`counts.test.js` passes.

---

## Phase P12 — Per-subject AI/voice bulk task entry (#11)

**Branch:** `feat/p12-ai-bulkadd` · **Depends on:** P9 merged (shares `SubjectDetail.jsx`) · **Size:** M–L

**Goal:** A per-subject prompt/mic box that turns "add lesson 18 to 36" (typed or dictated) into one
task per lesson in that subject — deterministic range parsing first, one batched write, a Gemini
fallback for freeform phrasing, and a confirm preview before writing.

**How it works today (context):** `geminiTools.js` declares 11 tools; `add_task` creates **one** task;
there is **no bulk tool**. `executeTool(name, args, ctx)` is a `switch`; `ctx = { uid, modeId, subjects,
habits, todos }` — **no active-subject notion** (subject is resolved from `args.subjectName`).
`chatWithGemini` loops **≤ 4 hops** (`gemini-flash-latest`), so a model-driven range like 18–36 (≈19
`add_task` calls) is impractical. ChatTab's slash commands bypass the model, calling `executeTool`
directly (works offline). `subjectService.addTask` uses `addDoc` one-at-a-time; there is **no
batch-*create* helper** — `reorderTasks` is the reusable `writeBatch` template (mint ids with
`doc(colRef)` then `batch.set`). `SubjectDetail` already has `uid`, `targetModeId`, `subject.id`,
`subject.name` in scope (no fuzzy matching needed). Voice = `useSpeechRecognition()` (`transcript`);
freeform single-shot NL can use `callAIProvider(prompt, systemInstruction)` (no tools). Task doc:
`{ title, column:'todo', priority:'medium', notes:'', dueAt:null, order, createdAt }` — **not**
encrypted.

**Files you OWN:** CREATE `src/lib/bulkParse.js` + `src/lib/__tests__/bulkParse.test.js` (pure range/list
→ `{ titles[], template, warning? }` or `null`); ADD `addTasksBulk(uid, modeId, subjectId, titles,
opts)` to `src/services/subjectService.js` (one chunked `writeBatch`, `batch.set(doc(tasksCol), {…})`,
chunk ≤ 400, recompute `progressPct` once, one ledger entry); CREATE
`src/components/subjects/SubjectQuickAdd.jsx` (text input + mic + preview/confirm); ADD an
`add_tasks_bulk` tool (declare + an `executeTool` case) to `src/services/geminiTools.js` (hotspot —
anchored addition only); EDIT (surgical, hotspot) `src/components/subjects/SubjectDetail.jsx` (mount
`<SubjectQuickAdd/>` above `MicroKanban`); optional EDIT `src/components/ai/ChatTab.jsx` (a `/tasks`
slash command → the bulk path).

**Files you must NOT touch:** `chatWithGemini`'s 4-hop loop (don't rely on it for the range — pass an
explicit title list), the todo encryption path, the other tools.

**Steps:**
1. `bulkParse.js` — pure extraction of a range/list + a template ("lesson 18 to 36", "1-5", "chapters
   3,5,7", "lectures 10–12") → a title array; returns `null` on non-matches (→ Gemini fallback). Tests
   for the matches and non-matches.
2. `addTasksBulk` — chunked batched multi-create; each `{ title, column:'todo', priority:'medium',
   notes:'', dueAt:null, order: Date.now()+i, createdAt }`; recompute `setSubjectProgress` once; one
   ledger + Island entry ("Added N tasks").
3. `SubjectQuickAdd.jsx` — text input + mic (`useSpeechRecognition().transcript`); on submit → `bulkParse`
   → **preview the N titles with a count** → confirm → `addTasksBulk`. If `bulkParse` is `null` and
   `hasGeminiKey()`, call `callAIProvider(…)` asking for a JSON title array (single-shot, not the 4-hop
   loop), parse → preview → write. No key + non-range → a single task, or a gentle "add a Gemini key for
   freeform" hint.
4. `add_tasks_bulk` tool + `executeTool` case (args `{ subjectName, titles[] }`); optional `/tasks` in
   ChatTab.
5. Docs: note the new tool + `addTasksBulk` in `claude.md`'s Gemini-tools section.

**Invariants:** free-tier (one batched commit, chunk ≤ 400 — not N round-trips); no key hardcoding;
graceful degradation without a key (the deterministic range path still works); **preview before write**
(no runaway creation); tasks stay unencrypted (as today).

**Done when:** typing or dictating "add lesson 18 to 36" in a subject creates 19 tasks in one batched
write after a confirm preview; freeform NL works when a Gemini key is set; the `add_tasks_bulk` tool +
`/tasks` command work in ChatTab; `bulkParse.test.js` passes.

---

## Phase P13 — Raster forest sprites on the calendar (#12)

**Branch:** `feat/p13-forest-sprites` · **Depends on:** none · **Size:** M

**Goal:** Render the owner's raster forest illustrations on the calendar. Crop each individual tree out
of the two sprite sheets into its own **transparent PNG**, background-remove the shrub band, and draw
those raster sprites at each calendar day-column baseline the way the previous/reference version did —
in place of the current hand-drawn SVG grove. The planted count/species still maps to that day's
successful focus sessions.

**How it works today (context):** `src/components/focus/CalendarForest.jsx` exports **SVG** components
`IllustratedOak/Pine/Blossom/Bush/Sapling`, the pure helpers `filterSuccessfulSessions` +
`getTreeTooltip`, and `DayGrove` — which renders 0/1/2/3+ session layouts of those SVGs (overlap,
z-layering, hover tooltips, grassy baseline). `DayGrove` is mounted **per day column** in
`TimetableGrid.jsx:654`, fed `sessions` + `dateStr`. Tests:
`src/components/focus/__tests__/CalendarForest.test.js`. The three source sheets in
`src/assets/forest/` — `trees.jpg` (5 trees on white), `trees-2.jpg` (4 trees on white, 7077×3109), and
`base-shrubs.jpg` (a horizontal foliage border with a **baked-in checkerboard** — JPEG has no real
alpha) — are **currently unused** in code. Combined = **9 distinct tree sprites** + the shrub band.

**Files you OWN:** CREATE cropped assets `src/assets/forest/trees/tree-01.png … tree-09.png` and
`src/assets/forest/shrubs/shrub-*.png` (individual transparent PNGs cropped + background-removed from
the three JPGs, produced by a repeatable script — see step 1); CREATE
`src/components/focus/ForestSprites.jsx` (a registry mapping species/size → the imported PNG url +
intrinsic aspect ratio, plus `<SpriteTree>` / `<SpriteShrub>` `<img>` renderers with the same
framer-motion pop-in, `origin-bottom drop-shadow-md select-none`); EDIT
`src/components/focus/CalendarForest.jsx` (swap `DayGrove`'s SVG tree/bush renders for the raster
sprites, keeping the exact placement/overlap/tooltip/session-count logic); EDIT
`src/components/focus/__tests__/CalendarForest.test.js` (update the render test; keep the pure-helper
tests). **Do not edit `TimetableGrid.jsx`** — the `<DayGrove>` mount already exists and passes
`sessions`/`dateStr` (this keeps P13 clear of P10's shelf ownership).

**Files you must NOT touch:** `TimetableGrid.jsx` (mount is already wired), `focusSlice` /
`useFocusEngine`, the focus-session doc shape.

**Steps:**
1. **Crop & background-remove (asset prep, repeatable script — not hand-cropping).** From `trees.jpg`
   (5) + `trees-2.jpg` (4) produce **9** tightly-cropped transparent tree PNGs; from `base-shrubs.jpg`
   produce 1–3 shrub PNGs with the checkerboard/white removed to alpha. Trim to content bounds and align
   trunk bottoms to a consistent baseline so they "plant" cleanly. Downscale the `trees-2` crops (7 k px
   source) to ≤ 512 px tall. Use ImageMagick (`-fuzz … -transparent white -trim +repage`) or `sharp`;
   record the source→output mapping in a short `src/assets/forest/README.md`. **Ship only the cropped,
   downscaled PNGs** — do not `import` the multi-MB source JPGs into the bundle.
2. **Sprite registry (`ForestSprites.jsx`).** `import` the PNGs (Vite fingerprints them), bucket them
   into the species `DayGrove` uses (oak/pine/blossom analogues chosen from the 9), and expose intrinsic
   width/height so the renderer preserves aspect ratio. `<SpriteTree species height delay/>` renders an
   `<img>` with the existing pop-in animation + `origin-bottom drop-shadow-md select-none`;
   `<SpriteShrub/>` renders a `base-shrubs` crop.
3. **Wire into `DayGrove`.** Replace the `<IllustratedOak/>` etc. calls with `<SpriteTree …/>` and the
   base bushes with `<SpriteShrub …/>`, preserving the 0/1/2/3+ layouts, overlap math, z-layering, hover
   tooltips (`getTreeTooltip`), and the grassy baseline. The species selection (`durationMin >= 45/60`)
   stays. Keep the SVG components exported as a fallback unless the owner asks to retire them.
4. **Bundle discipline.** Confirm `npm run build` doesn't balloon (transparent + downscaled crops, no
   `import * as`); the raw JPGs stay unimported.
5. Tests: keep the pure-helper tests unchanged; adjust the render test to assert one sprite per
   successful session and that shrubs render at the base.

**Invariants:** the session→tree count/species mapping is unchanged; free-tier (assets are static, no
reads/writes); no `import * as`; PNGs must be genuinely transparent (no white halo / checkerboard) so
they sit cleanly on the calendar; keep files feature-scoped.

**Done when:** build gates green; each calendar day column shows the raster trees + base-shrubs at its
baseline (matching the reference look), one tree per successful session, tooltips intact; the source
sprite sheets are cropped into individual transparent PNGs; the bundle stays reasonable.

---

## Cross-cutting verification (run after each phase, and once at the end)

1. **Build gates:** `npm run lint`, `npm test`, `npm run build`, and Electron build (`ELECTRON=true vite build`) — all green.
2. **Desktop smoke (Electron):** launch, sign in, switch modes incl. "All Scopes", start/lock/PiP a focus session, trigger a check-in and a quote (center-blur), fire a toast (chime), open Settings.
3. **Free-tier audit:** confirm no new always-on/global listeners and no Cloud Function/server dependency was introduced (grep for `functions`, new `onSnapshot` without teardown).
4. **Docs:** update `claude.md` for any new settings/fields/flows your phase added (esp. P2's calendar fields, P4's promptSlice, P7's navSlice/tags).
5. **Changelog:** add a `content/changelog.js` entry and a `## v2.1.x — YYYY-MM-DD` heading in `CHANGELOG.md` (do not overwrite existing entries; the release workflow reads this heading format).
6. **P9–P13 smoke:** scroll through *all* subjects in the compact card; add a subject from compact and from "All Scopes" (mode picker); open a subject and confirm the sibling rail stays and switches with one click; the timetable top to-dos are compact markers with a side detail pane on hover; an incomplete overdue to-do appears on *today*; the calendar-header counter is live and clickable; a subject quick-add turns a lesson range (e.g. 18–36) into that many tasks in one batched write after a preview; the calendar day columns show the raster tree/shrub sprites (no white halo).

---

## Appendix — quick file index (verified during exploration)

- Store: `src/store/useStore.js` (11 slices: ui, mode, user, focus, chrome, island, chrono, update, patreon, checkin, lock).
- Notifier: `src/store/slices/islandSlice.js` (`pushIsland`), `src/hooks/useIslandCycle.js`, `src/components/island/DynamicIsland.jsx`.
- Focus: `src/store/slices/focusSlice.js`, `src/hooks/useFocusEngine.js`, `src/components/focus/*` (PipAppView, FocusLockScreen, FocusMiniOverlay, FloatingFocusPip, PipFocusWindow, SessionCompleteModal, ForestView, CalendarForest).
- Electron: `electron/main.js` (single window; `pip:enter`/`pip:exit`; window-state.json), `electron/preload.js` (`window.protrack`), `src/lib/pip.js`, `src/hooks/useDesktopIntegration.js`.
- Timetable/calendar: `src/components/timetable/*` (TimetableGrid, TodayAgenda, SlotEditorModal, TimeContextPanel), `src/components/widgets/TimetableWidget.jsx`, `src/services/timetableService.js`, `src/services/calendarService.js`, `src/components/auth/LinkGcalPage.jsx`, `src/lib/time.js`, `src/lib/deadlines.js`, `src/lib/dates.js`.
- Check-ins/quotes: `src/hooks/useCheckIns.js`, `src/lib/checkin.js`, `src/components/checkin/CheckInCard.jsx`, `src/services/checkinService.js`, `src/components/common/ZenOverlay.jsx`, `src/hooks/useIdleDetection.js`.
- Reminders: `src/hooks/useHabitReminders.js`, `src/components/wellness/HabitReminderToast.jsx`, `src/hooks/useDeadlines.js`, `src/hooks/useNoteReminders.js`, `src/components/wellness/HydrationReminder.jsx`.
- Audio/TTS: `src/lib/audioFX.js`, `src/lib/audioEngine.js`, `src/components/ai/HandsFreeTab.jsx`, `src/components/ai/BackgroundHandsFree.jsx`.
- Nav/scope: `src/components/layout/{Dashboard,BoardCanvas,ModeSwitcher,Workspace,TopBar}.jsx`, `src/store/slices/{uiSlice,modeSlice}.js`, `src/components/widgets/widgetRegistry.js`.
- Services: timetable, todo, subject, focus, habit, note, mode, user, onboarding, calendar, deviceLink, device, gemini(+Tools), ledger, checkin, lock, crypto, subscriptionCache, patreon.
- **v2.1.x follow-up (P9–P13) new files:** `src/lib/{bulkParse,counts}.js` (+ tests), `src/components/timetable/ItemDetailPopover.jsx`, `src/components/subjects/SubjectQuickAdd.jsx`, `src/components/focus/ForestSprites.jsx`, cropped assets under `src/assets/forest/{trees,shrubs}/*.png`. Reused/edited: `src/hooks/useSpeechRecognition.js`, `src/services/geminiTools.js`, `src/components/ai/ChatTab.jsx`, `src/components/focus/CalendarForest.jsx` (`DayGrove`, mounted in `TimetableGrid.jsx:654`), `src/lib/{deadlines,dates,dayAgenda}.js`, `src/components/widgets/{SubjectsWidget,TodosWidget,TimetableWidget}.jsx`, `src/components/subjects/{SubjectDetail,SubjectEditorModal}.jsx`. Source forest sprite sheets: `src/assets/forest/{trees,trees-2,base-shrubs}.jpg`.
