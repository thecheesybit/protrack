rules_version = '2';

// Audio notes and user uploads live under users/{uid}/… — owner-only access.
service firebase.storage {
  match /b/{bucket}/o {
    match /users/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null
        && request.auth.uid == userId;
    }
  }
}
